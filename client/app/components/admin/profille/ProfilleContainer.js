import { connect } from 'react-redux';
import Profille from './Profille.js';

function mapDispatchToProps() {
  return {};
}

function mapStateToProps() {
  return {};
}

export default connect(mapStateToProps, mapDispatchToProps)(Profille);